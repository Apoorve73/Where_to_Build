import React from "react"

const NavbarTiles = (props) => {
    return(
        <div>
            
                { /*eslint-disable-next-line */} 
                <li><a href="#">{props.chore}<span></span><span></span></a></li>
            
        </div>
    );
}

export default NavbarTiles;